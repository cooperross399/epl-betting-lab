# Codex Prompt — Add Shots/SOT Model

Read `AGENTS.md` and follow it.

Task:
Add a starter shots and shots-on-target model using Football-Data columns:

- `HS` = home shots
- `AS` = away shots
- `HST` = home shots on target
- `AST` = away shots on target

Requirements:

1. Add team-level shot-for and shot-against tendencies.
2. Add projected shots and SOT for each fixture.
3. Add dashboard tables.
4. Add manual odds support later only if the user provides prop lines.
5. Do not auto-bet player props without lineup confirmation.
6. Run compile checks and tests.
